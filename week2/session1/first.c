#include <stdio.h> //include standard IO library

int main(){ //define the main function
    int a = 10;
    printf("%d\n", a); //format prints the value as an integer
    return 0; //returns function with error code 0
}